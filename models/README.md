encoder.py
    feed forward layers (w/ increasing dim)
    creates latent space
    output higher dim latent representation

decoder.py
    input latent representation
    feed forward layers (w/ decreasing dim)
    decode latent space